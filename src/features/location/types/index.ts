// Location types
export interface LocationData {
  id: string;
  name: string;
  code: string;
}

export interface AddressData {
  provinceName: string;
  provinceCode: string;
  wardName: string;
  wardCode: string;
  detail: string;
}
